/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package BUS;

import javax.swing.table.DefaultTableModel;

/**
 *
 * @author nhatm
 */
public class PhanQuyenBUS {
//    public void loadPermissionsFromDatabase(DefaultTableModel model) {
//        List<Permission> permissions = permissionDAO.getPermissions();
//
//    // Duyệt qua từng hàng của bảng và cập nhật giá trị checkbox
//        for (int i = 0; i < permissions.size(); i++) {
//            Permission perm = permissions.get(i);
//            model.setValueAt(perm.isView(), i, 1);
//            model.setValueAt(perm.isAdd(), i, 2);
//            model.setValueAt(perm.isEdit(), i, 3);
//            model.setValueAt(perm.isDelete(), i, 4);
//            }
//    }
}
